language: node_js

# 指定 Node.js 版本，建議使用穩定版本
node_js:
  - "22"

# 觸發條件：當有 PR 或是推送到指定分支時
branches:
  only:
    - main
    - develop

# 快取設定，加速後續執行
cache:
  directories:
    - node_modules

# 執行步驟
install:
  - npm ci

script:
  - npm run test -- --run
